{
  "name": "trading",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "docker:start": "sudo docker compose up",
    "docker:stop": "sudo docker compose stop",
    "docker:clean": "sudo docker compose rm -fsv"
  }
}
